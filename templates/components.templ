package templates

import (
	"github.com/0xb0b1/blog/i18n"
	"github.com/0xb0b1/blog/models"
	"fmt"
)

templ PostCard(post models.Post, lang i18n.Lang) {
	<div class="post-card">
		<h2 class="post-card-title"><a href={ templ.SafeURL("/" + string(lang) + "/posts/" + post.Slug) }>{ post.Title }</a></h2>
		<div class="post-meta">
			<span class="post-date">{ formatDate(post.Date, lang) }</span>
			<span class="post-reading-time">{ fmt.Sprintf("%d min read", post.ReadingTime) }</span>
		</div>
		<p class="post-description">{ post.Description }</p>
		<div class="post-tags">
			for _, tag := range post.Tags {
				<span class="tag">{ tag }</span>
			}
		</div>
	</div>
}
